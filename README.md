A basic image editor that was a class project aimed around applying the classes' teachings of Java concepts.

To run simply download the pm.ppm image (or any ppm image) and then run the ImageEditorRunner.
